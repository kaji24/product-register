sudo : required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docker docker-compose exec --env 'RAILS_ENV=test' web rails db:create
  - docker docker-compose exec --env 'RAILS_ENV=test' web rails db:migrate
  - docker docker-compose exec --env 'RAILS_ENV=test' web rails test